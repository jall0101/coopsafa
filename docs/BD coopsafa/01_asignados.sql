DROP TABLE IF EXISTS `asignados`;

CREATE TABLE `asignados` (
  `idasignados` int NOT NULL,
  `nombre` varchar(45) NOT NULL,
  `asignadoDepartamento` varchar(45) NOT NULL,
  PRIMARY KEY (`idasignado`)
)
